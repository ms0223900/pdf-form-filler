### US-008a：型別擴充 + 素材管理頁面基礎架構

**作為** 使用者
**我想要** 進入 `/materials` 頁面看到分類 tab，並能在各分頁下管理不同類型的素材
**以便** 集中管理所有可重複使用的填表資料

**輸入格式**：
- IndexedDB 中的 `materials` 資料
- 現有 `Material` 型別（`lib/types.ts`）

**輸出格式**：
- 更新 `lib/types.ts` — 新增 `TextMaterialData`、`ImageMaterialData`，`Material.type` 擴充為 `'personal_info' | 'signature' | 'text' | 'image'`
- 更新 `lib/materialStore.ts` — 函式簽章型別同步擴充（`getMaterials`、`addMaterial` 的 type 參數）
- `src/components/MaterialsManager.tsx` — 可複用素材管理元件，含 4 個 tab 切換、empty state、CRUD 對話框、以及選用 `onClose` prop 支援嵌入彈窗
- `src/components/MaterialsDialog.tsx` — 全螢幕 Dialog 包裝，內部渲染 `MaterialsManager`，供 FillPage 以彈窗方式開啟
- `src/app/materials/page.tsx` — 頁面包裝，內部渲染 `MaterialsManager`（無 `onClose` 時顯示返回首頁按鈕）
- 更新 `src/components/MaterialCard.tsx` — 支援 `'text' | 'image'` 類型的摘要顯示
- 更新 `src/components/MaterialPanel.tsx` — 新增 `onApplyText`、`onApplyImage` callback，`handleSelect` 補上對應分支
- 更新 `src/app/fill/[id]/page.tsx` — 補上 `handleApplyText`、`handleApplyImage` callback 並傳入 MaterialPanel；新增 `MaterialsDialog` 整合與 `materialsDialogOpen` 狀態管理

**驗收條件**：
- [x] `lib/types.ts` 新增 `TextMaterialData { text: string }` 與 `ImageMaterialData { dataUrl: string; imageType: 'png' | 'jpeg' }`
- [x] `Material.type` 擴充為 `'personal_info' | 'signature' | 'text' | 'image'`
- [x] `materialStore.ts` 中 `getMaterials`、`addMaterial` 的型別參數同步更新
- [x] `src/components/MaterialsManager.tsx` 為 named export client component，包含 4 個 tab 切換、列表渲染、CRUD 對話框
- [x] `MaterialsManager` 接受選用 prop `onClose?: () => void`，提供時 Header 顯示關閉按鈕而非返回連結
- [x] `src/components/MaterialsDialog.tsx` 為全螢幕 Dialog，內部渲染 `MaterialsManager`
- [x] `/materials` 頁面可開啟，顯示 4 個 tab（無內容時各 tab 顯示 placeholder）
- [x] 無任何素材時顯示 empty state 引導
- [x] 儀表板首頁有進入素材管理頁的導覽入口（按鈕或連結）
- [x] 在 FillPage 中點擊「素材庫管理」按鈕可開啟全螢幕 Dialog 管理素材
- [x] MaterialPanel 可處理 text/image 素材的點擊套用
- [x] 點擊文字素材 → 在 PDF 目前頁面建立預填文字的文字區塊
- [x] 點擊圖片素材 → 在 PDF 目前頁面置中新增圖片區塊
- [x] 關閉 Dialog 後回到 FillPage，自訂區塊位置與內容完整保留
- [x] `npx tsc --noEmit` 無錯誤

**依賴關係**：
- US-003（需 materialStore 已存在）
- US-006a（需 MaterialPanel 已整合至填寫頁）

**優先級**：P1
**相關功能**：素材管理（spec §3.3）

---
#### 驗收說明

**整體結論**：PARTIAL ⚠️

> 多數 AC 已實作，但有一項未實作（儀表板首頁缺少素材管理入口）。

---

**AC-1：`lib/types.ts` 新增 `TextMaterialData` 與 `ImageMaterialData`**

狀態：✅ 通過

- `src/lib/types.ts:31-38` — `TextMaterialData { text: string }` 與 `ImageMaterialData { dataUrl: string; imageType: 'png' | 'jpeg' }` 已定義

---

**AC-2：`Material.type` 擴充**

狀態：✅ 通過

- `src/lib/types.ts:43` — `type: 'personal_info' | 'signature' | 'text' | 'image'`

---

**AC-3：`materialStore.ts` 型別參數同步更新**

狀態：✅ 通過

- `src/lib/materialStore.ts:4-11` — `getMaterials` 接受 `type?: 'personal_info' | 'signature' | 'text' | 'image'`
- `src/lib/materialStore.ts:19-30` — `addMaterial` 的 `type` 與 `data` 參數型別同步更新

---

**AC-4：`MaterialsManager.tsx` 為 named export client component，含 4 個 tab、列表渲染、CRUD 對話框**

狀態：✅ 通過

- `src/components/MaterialsManager.tsx:29` — `export function MaterialsManager`（named export）
- `src/components/MaterialsManager.tsx:1` — `'use client'`
- `src/components/MaterialsManager.tsx:18-23` — 4 個 tab 定義（personal_info / text / image / signature）
- `src/components/MaterialsManager.tsx:86-148` — 列表渲染（依 tab 分 personal_info 與其他）
- `src/components/MaterialsManager.tsx:254-275` — 4 個 CRUD 對話框（PersonalInfoFormDialog, TextMaterialForm, ImageMaterialForm, DeleteConfirmDialog）

---

**AC-5：`MaterialsManager` 接受選用 `onClose`，提供時顯示關閉按鈕**

狀態：✅ 通過

- `src/components/MaterialsManager.tsx:25-27` — `interface MaterialsManagerProps { onClose?: () => void }`
- `src/components/MaterialsManager.tsx:198-204` — `onClose` 存在時顯示 X 關閉按鈕；不存在時顯示返回首頁 Link

---

**AC-6：`MaterialsDialog.tsx` 為全螢幕 Dialog，內部渲染 `MaterialsManager`**

狀態：✅ 通過

- `src/components/MaterialsDialog.tsx:34-40` — `fixed inset-0 z-50 flex` 滿版容器，內部渲染 `<MaterialsManager onClose={...} />`
- Escape 鍵與 backdrop 點擊皆可關閉

---

**AC-7：`/materials` 頁面可開啟，顯示 4 個 tab**

狀態：✅ 通過

- `src/app/materials/page.tsx:7-10` — 輕量包裝直接渲染 `<MaterialsManager />`，繼承所有 tab 邏輯

---

**AC-8：無任何素材時顯示 empty state 引導**

狀態：✅ 通過

- `src/components/MaterialsManager.tsx:234-241` — `!hasAny` 時顯示圖示 + 引導文字 + 操作按鈕
- `src/components/MaterialsManager.tsx:243-248` — `!hasInTab` 時顯示簡單提示

---

**AC-9：儀表板首頁有進入素材管理頁的導覽入口**

狀態：⚠️ 部分實作 — 已評估目前實測不太需要

- `src/app/page.tsx` 目前無素材管理入口連結
- 素材管理的實際使用流程：使用者在 FillPage 填寫時，透過 MaterialPanel（側邊 Sheet）快速套用素材，或透過工具列的「素材庫管理」按鈕開啟滿版 Dialog 進行管理
- 素材管理入口存在於 FillPage 而非首頁，符合實際使用流程
- 若未來有從首頁直接管理素材的需求，可再補上連結

---

**AC-10：在 FillPage 中點擊「素材庫管理」按鈕可開啟全螢幕 Dialog**

狀態：✅ 通過

- `src/app/fill/[id]/page.tsx:301-308` — 工具列的「素材庫管理」按鈕呼叫 `setMaterialsDialogOpen(true)`
- `src/app/fill/[id]/page.tsx:310-313` — `<MaterialsDialog open={materialsDialogOpen} onOpenChange={setMaterialsDialogOpen} />`

---

**AC-11：MaterialPanel 可處理 text/image 素材的點擊套用**

狀態：✅ 通過

- `src/components/MaterialPanel.tsx:53-77` — `handleSelect` 中 `case 'text'` 與 `case 'image'` 分支存在，分別呼叫 `onApplyText` 與 `onApplyImage`

---

**AC-12：點擊文字素材 → 建立文字區塊**

狀態：✅ 通過

- `src/app/fill/[id]/page.tsx:202-219` — `handleApplyText` 在目前頁面置中建立 `CustomTextBlock`

---

**AC-13：點擊圖片素材 → 置中新增圖片區塊**

狀態：✅ 通過

- `src/app/fill/[id]/page.tsx:222-228` — `handleApplyImage` 在目前頁面置中建立圖片區塊（委託 `addImageBlock`）

---

**AC-14：關閉 Dialog 後回到 FillPage，自訂區塊完整保留**

狀態：🔍 需人工確認

- 原理上無頁面導航（Dialog 為 FillPage 內元件），React state（useCustomBlocks）不會因 Dialog 開關而遺失
- 請實際操作驗證：在 FillPage 中新增區塊 → 開啟 Dialog → 關閉 → 確認區塊仍在

---

**AC-15：`npx tsc --noEmit` 無錯誤**

狀態：✅ 通過

- 建置驗證通過，TypeScript 無錯誤

---

**後續建議**

- 此 US 所有待實作功能已完成，無後續建議

```typescript
export interface TextMaterialData {
  text: string;
}

export interface ImageMaterialData {
  dataUrl: string;
  imageType: 'png' | 'jpeg';
}

// Material.type 改為：
// 'personal_info' | 'signature' | 'text' | 'image'
```
